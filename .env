PORT=3000
MONGO_URI=mongodb://127.0.0.1:27017/notificationSystem 
SUPERADMIN_ROLE = 'SUPERADMIN';
SUPERADMIN_PASSWORD = "admin123"
SUPERADMIN_EMAIL = "superadmin@gmail.com"
API_BASE= "api/v1"
JWT_SECRET=10




# - Blocking users is incomplete: wrong validation target, uses GET to mutate data, does not await the update, and may return success even when the update fails.
# - Frontend lint currently fails with 14 errors; notification bell state order and management-page effects are notable issues.
# - Registration has no success/error UX or redirect.
# - There are no automated tests.
# - Both .env files are tracked by Git, which risks exposing secrets.